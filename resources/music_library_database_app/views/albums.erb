<html>
  <head>
    <title>Albums</title>
  </head>
  <body>
    <h1>Albums</h1> 
    <% @albums.each do |album| %>
      <a href='/albums/<%= album.id %>'><%= album.title %></a>
      <p>Released: <%= album.release_year %></p>
    <% end %>
  </body>
</html>